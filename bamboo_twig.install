<?php

/**
 * @file
 * Contains bamboo_twig.install.
 */

/**
 * Implements hook_install().
 */
function bamboo_twig_install() {
  $messenger = \Drupal::messenger();
  $messenger->addMessage(t('Bamboo Twig is installed and ready for action'));
}
